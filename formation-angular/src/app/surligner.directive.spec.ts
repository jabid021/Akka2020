import { SurlignerDirective } from './surligner.directive';

describe('SurlignerDirective', () => {
  it('should create an instance', () => {
    const directive = new SurlignerDirective();
    expect(directive).toBeTruthy();
  });
});
